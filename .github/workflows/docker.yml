name: Docker

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  docker:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image:
          - name: lyre-api
            target: api
          - name: lyre-web
            target: web
    steps:
      - uses: actions/checkout@v5
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/5aaee9/lyre/${{ matrix.image.name }}
          tags: |
            type=ref,event=branch,enable=${{ github.event_name == 'push' }}
            type=ref,event=tag
            type=raw,value=main,enable=${{ github.event_name == 'workflow_dispatch' }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          target: ${{ matrix.image.target }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: Cleanup Untagged Image
        uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          packages: lyre/${{ matrix.image.name }}
          delete-untagged: true

  helm-chart:
    runs-on: ubuntu-latest
    needs: docker
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref_type == 'tag' && startsWith(github.ref_name, 'v'))
    steps:
      - uses: actions/checkout@v5
      - uses: azure/setup-helm@v4
      - name: Log in to GHCR for Helm
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io --username "${{ github.actor }}" --password-stdin
      - name: Resolve chart version
        id: chart-version
        shell: bash
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" && "${GITHUB_REF_NAME}" == v* ]]; then
            version="${GITHUB_REF_NAME#v}"
          else
            version="0.1.0-main"
          fi
          echo "version=${version}" >> "$GITHUB_OUTPUT"
      - name: Package Helm chart
        run: |
          helm package scripts/kubernetes \
            --destination dist \
            --version "${{ steps.chart-version.outputs.version }}" \
            --app-version "${{ steps.chart-version.outputs.version }}"
      - name: Push Helm chart to GHCR
        run: helm push dist/lyre-*.tgz oci://ghcr.io/5aaee9/lyre/chart
      - name: Cleanup Untagged Chart
        uses: dataaxiom/ghcr-cleanup-action@v1
        with:
          packages: lyre/chart/lyre
          delete-untagged: true
